[user]
	name = Rob Peters
	email = rjpcal@gmail.com
[push]
	default = current
[color]
	diff = auto
	status = auto
	branch = auto
	ui = auto
[credential]
	helper = cache --timeout=1200
[pull]
	rebase = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
